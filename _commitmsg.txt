feat(coach): add per-workout RPE + note; feed RPE/note/day-tags into the prompt

Schema (run by user in Supabase): workouts.note text already existed;
added workouts.rpe smallint CHECK (rpe BETWEEN 1 AND 10).

- workouts DAL: map note → note, rpe → rpe (rpe in INT_FIELDS; empty → null so the CHECK never rejects)
- ActivityForm: optional RPE (1–10, with a plain-language hint explaining the scale) + free-text note, both for every activity type; RPE clamped to 1–10-or-null on save
- coachPrompt: recent-activity lines now carry RPE + note; new [Recent Day Notes] block surfaces the last 21 days of calendar day-tags (sick / travel / poor sleep / massage / stretching) to the coach
- thread dailyNotes into buildDataBlock at both call sites (sendChat + AICoachTab preview)
- docs: training-log + ai-coach + changelog

Verified end-to-end on the dev preview: saved a workout with RPE7 + note, confirmed it round-trips through the DB and appears in the assembled prompt, then deleted it.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
