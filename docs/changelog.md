# 更新日志

> Ultreia 的"装修日志" —— 从 2026-05-13 闲来无事点了下 artifacts 开始，到今天每一天加了什么、改了什么、拆了什么。

## 2026-07-06

- **底部 tab 与审核行再收紧**：移动端底部 tab 冷切换和收件箱 tab 同步去掉额外的 0ms 二段延迟，Memory 更新审核条目改为整行安全 tap，连续勾选时不再等原生 checkbox change。
- **导入与计划审核选择提速**：上传活动复盘里的天气 / AI Coach 点评开关、AI Coach 计划导入里的计划勾选，以及日历计划里的主课开关改为安全 tap，本地选择抬手即响应，同时保留滑动误触保护。
- **AI Coach 消息小按钮提速**：消息里的导入日历 / 停止提取、重新发送按钮改为安全 tap，滑动消息时不会误触，点按抬手后不再等普通 click；图片附件入口也改为按下即打开。
- **收件箱和周报设置继续提速**：收件箱每日推送 / 周报 / 其他 tab 先完成本地切换绘制，再同步 App 顶层状态；周报设置里的自动生成和周日导入提醒开关改为按下即响应。
- **底部 tab 冷启动点击拆帧**：第一次点到尚未挂载的底部 tab 时，先绘制底部高亮和触感反馈，再切换目标页内容，避免重页面临时挂载挡住按下响应；已预热页面仍保持直接切换。
- **轻量表单入口继续提速**：赛事表单里的地点清空 / 搜索结果选择，以及训练 / 赛事长按菜单里的编辑入口改为即时响应；保存、删除、提交等动作仍保留原确认与点击路径。
- **即时点击去重更稳**：底部 tab、收件箱 tab、AI Coach 设置 / token / Memory 行、全局确认弹窗和通用即时按钮的 pointer/click 去重更稳定，减少触摸已响应后又被合成 click 干扰，或页面刚加载时首次点击被误吞的情况。
- **训练与赛事卡片短按响应提速**：训练卡片展开 / 多选、赛事卡片天气展开从普通 click 提前到 touchend 阶段响应，同时保留长按操作菜单和滑动误触保护。
- **AI Coach 轻量按钮与确认弹窗继续提速**：AI Coach 图片附件移除、教练提示继续、手动调整确认弹窗关闭改为按下即响应；通用确认弹窗的关闭、取消和确认按钮也改为即时响应，但不会跳过确认步骤。
- **日历与训练卡片触控响应补齐**：日历格子改为安全 tap 打开日期详情，训练移动端卡片补充触控优化，减少点日期、点训练卡片时等普通 click 的慢半拍。
- **收件箱与 Memory 审核行响应补齐**：收件箱每日推送 / 其他消息行改为安全 tap 标记已读，AI Coach Memory facts 的接受、拒绝、恢复按钮改为按下即响应；删除、归档等需要确认或会写入的动作仍保留原触发方式。
- **移动端按钮反馈过渡压缩**：移动端按钮的颜色、边框、背景等状态过渡统一缩短，减少 tab / chip / 小按钮已经触发但视觉选中态慢半拍的感觉。
- **更多设置与动作按钮即时响应**：Settings 行按钮、AI Coach 菜单 / Memory 更新 / 当前焦点动作、收件箱 Clear all 和确认删除按钮改为 pointer/tap 阶段触发，减少等 click 合成事件带来的轻微滞后。
- **训练页高频筛选首帧提速**：Training 的 Activities / Charts 切换、Races 移动页签与赛事类型筛选、全局类型筛选和周期筛选都会先更新本地选中态 / 菜单关闭，再下一帧同步父级状态，减少列表和图表重算挡住点按反馈。
- **收件箱与 Coach 设置 chip 响应继续收紧**：收件箱每日推送 / 周报 / 其他 tab 点按后先本地切换，再下一帧同步 App 顶层状态；AI Coach 设置 hub 和 Memory 分类 / 当前归档 chip 不再延后实际内容切换，减少高亮已变但内容慢半拍的情况。
- **底部 tab 首帧响应继续优化**：移动端底部 tab 点按会先完成本地页面切换和高亮绘制，再在下一帧同步 App 顶层 tab 状态，减少父级重渲染挡住点按首帧反馈的情况；横滑结算仍保持即时同步。
- **底部 tab 预热优先级再调整**：移动端底部 tab 的空闲预热不再用低优先级更新，预热等待窗口也缩短，减少开屏后第一次跳到未访问 tab 时仍要临时挂载页面的慢半拍。
- **底部 tab 点击挂载范围收窄**：点击底部 tab 时，同步阶段只挂当前页和目标页，不再同时挂目标页左右相邻页；横滑手势仍保留宽窗口，减少第一次跨 tab 点按被额外页面渲染拖慢。
- **底部 tab 与轻量弹窗继续提速**：移动端底部 tab 的后台预热提前启动，减少刚进 App 后第一次跨 tab 跳转要临时挂载页面的慢半拍；AI Coach 训练偏好清空、日历日期 / 计划弹窗关闭、顶部记忆 / 调整提醒、周报导入提示稍后、教练点评提示取消、计划导入草稿移除、位置草稿取消、使用手册章节切换、账号菜单轻量入口、赛事过期提醒取消、地图 / 周报 / 提示词 / 邀请码等浮层关闭、月报海报关闭和 ITRA 小标编辑也改为按下即响应，保存 / 导入 / 下载 / 删除 / 退出登录仍保留普通点击。
- **导入提醒取消提速**：CSV / FIT 导入遇到重复活动或未知运动类型时，提醒里的取消按钮改为按下即关闭；跳过重复、仍然导入和确认映射继续保留普通点击。
- **底部 tab 和收件箱 tab 的真实状态同步提速**：移动端底部 tab 点击时先同步落地可见 tab / 底部高亮 / 目标页面挂载，再更新 App 的真实 tab；收件箱每日推送 / 周报 / 其他 tab 也改为同步更新 App 状态，减少“视觉点中了但实际状态慢一拍”的情况。
- **AI Coach 设置状态同步提速**：教练风格、回复长度、干预强度、日历按钮、训练偏好和夜间 Memory 审核等设置不再低优先级同步到 App 状态，减少关闭弹窗或马上继续操作时配置慢一拍。
- **训练 / 赛事页内切换与筛选同步提速**：训练记录 / 图表切换、赛事 / PR 切换、目标 / 历史切换、周期选择和全局类型筛选不再低优先级同步到 App 状态，减少 chip 高亮和实际内容状态不一致的慢半拍。
- **账号与天气设置关闭继续提速**：修改密码、删除账号、天气 API 和天气自动更新设置里的关闭 / 取消按钮改为按下即响应，删除账号前的备份提示展开 / 收起也同步提速；保存、更改密码、删除账号、导出备份和清空 token 等真实动作仍保留普通点击。
- **设置类弹窗本地操作继续提速**：Settings 语言开关、收件箱里的每日推送 / 周报设置关闭与取消、每日推送时间删除、每日状态提示关闭 / 跳过改为按下即响应；保存设置和保存状态仍保留普通点击。
- **设置弹窗关闭响应提速**：AI Coach 设置里的教练偏好、训练偏好、当前焦点、日历按钮、最近建议、Memory、Prompt Preview，以及收件箱和导入审核弹窗关闭 / 取消改为按下即响应；确认导入、生成、保存、删除、清空等动作不变。
- **图表点选和浮层关闭继续提速**：图表趋势点、训练负荷点、Coach 提示跳转、已有一键调整 / 赛前简报打开、教程 / 赛前简报弹窗关闭改为按下即响应；生成、发送、保存、删除、清空等真实任务动作不变。
- **边缘入口继续提速**：账号下拉、首次引导前后页、API / 天气教程入口、位置地图入口、图表海报入口、月报字段 chip 和 AI Coach 顶部 token 浮窗入口改为按下即响应；退出、删除、保存、定位、下载等真实任务动作继续保留普通点击。
- **更多轻量控件即时响应**：使用手册目录、AI Coach 计划导入详情 `!`、钱包说明 / 周期 chip、位置地图缩放按钮改为按下即响应；刷新、定位、充值、保存等真实任务按钮仍保留普通点击。
- **移动端 tab 空闲预热**：首屏后会在空闲时间预挂载 5 个底部 tab，并在预热完成后保留已挂载页面，减少第一次跳到非相邻 tab 时才临时渲染造成的慢半拍。
- **移动端按键实际响应优化**：底部 tab 点击不再在按下路径里同步渲染重页面，先更新选中态、页面位置和真实 tab，再让其余页面在后台预热；AI Coach 设置、Memory 分类和收件箱 tab 改为先本地响应，再延后较重的列表重算，减少点按后慢半拍的感觉。
- **选择类控件继续提速**：下拉框触发器、训练页周期 / 类型筛选 / Activities-Charts 切换、Races 移动端页签和非破坏性 chip、AI Coach 设置入口与 Memory chip 改为触屏按下即响应；保存、删除、发送和可滚动列表行仍保留普通点击，避免误触。
- **底部 tab 与可滚动 chip 再提速**：底部 tab 点按时只准备目标页窗口，不再额外渲染中间页；周报周期行、其他收件箱行和训练周期下拉长列表改为安全 tap，手指滑动时不误触，点按抬手后立即响应。
- **高频小按钮继续提速**：训练表单的跑步类型 / 标签 / 力量部位 chip、日历月份导航、图表周期 chip、AI Coach 顶部天气 / 位置 / 收件箱 / 手动调整入口改为按下即响应；保存、删除、发送等动作仍保留普通点击。
- **设置与导入审核继续提速**：Settings 账号折叠、非危险设置行、设置分组 chip、每日推送 / 周报设置的信息与时间选择、导入审核页码 / 力量部位 chip、每日自评 chip 响应更快；退出、删除、清缓存、保存、确认导入等动作仍保留普通点击。
- **日历和 Coach 审核控件继续提速**：日历日期详情里的日期切换、日标签、晨间状态、计划类型 chip、AI Coach 计划导入审核 chip、天气更新频率、Memory 分类展开和最近教练建议行响应更快；危险操作和确认保存继续保留普通点击。
- **剩余高频入口继续提速**：宽屏顶部 tab / 顶部入口、通用下拉选项、训练页添加 / 选择 / 加载更多和时间滚轮确认栏响应更快；文件上传、删除、保存、发送等高风险或系统选择器动作仍保留普通点击。
- **AI Coach 细节控件继续提速**：加载更早消息、上下文用量提示展开 / 收起、回到顶部 / 最新、Prompt 语言切换和单列滚轮确认栏响应更快；发送、生成、清空、删除等真实任务动作仍保留普通点击。
- **底部 tab 主路径再提速**：底部 tab 点按后先完成本地选中态和页面位移，减少重页面渲染挤占按下响应；Races 添加入口也改为按下即打开。

## 2026-07-05

- **按钮响应与保存反馈优化**：全局按钮按下反馈改为更即时；每日推送、周报、天气、训练、日历计划、赛事、登录和密码相关保存入口补充“保存中 / 登录中”旋转反馈，并在写入期间禁用重复点击。AI Coach 收件箱的每日推送 / 周报 / 其他 tab 改为先本地切换再同步上层状态，减少点按后慢半拍的感觉。
- **开屏 logo 动画去闪烁**：山峰和山路出现后不再先压暗再突然补亮；最终 logo 改为平滑接管构建层，去掉类似“按一下”的闪动感。
- **开屏 logo 动画贴合真实轮廓**：开屏构建层不再用近似曲线和两个三角去模拟 logo；背景等高线改为从真实 logo 底图里自然浮现，山峰和山路也从真实 logo 裁切淡入，减少动画形状和最终 logo 不一致的割裂感。
- **Memory 审核更保守**：长期记忆更新会把斜杠、标点和“或 / and / or”这类轻微表达差异视为同一事实，不再因为措辞微调要求更新。AI 漏掉的旧事实只显示为“可选归档”，默认不勾选，避免一键接受时误归档仍然有效的记忆。
- **AI Coach 输入框修复**：手机系统语音输入同一句话不再重复追加；AI Coach 移动端输入框删除文字时会保住当前光标和滚动位置，减少删着删着跳行。周复盘讨论输入也加入同一套语音重复保护。
- **移动端 Tab 切换与横滑修复**：底部 Tab 快速点击或反向横滑不再误跳到训练 / 设置，图标、选中胶囊和文字高亮同步更新；日历、AI Coach、训练图表和比赛个人记录之间的横滑边界交接更稳定，AI Coach 对话区恢复纵向滚动，比赛卡片横滑时不再误触发长按弹窗。Training / Races 内部二级 Tab 也加入轻量 slide + fade，避免硬切。

## 2026-07-04

- **导入自评输入修复**：完成导入弹窗里的主观感受输入框明确按普通文本输入处理，减少安卓九宫格拼音被当成整组字母直接写入的问题。
- **每日自查跨端弹窗修复**：PWA / App 打开时会等云端每日自查记录加载完成后再判断是否弹窗，避免 App 已填写但 PWA 首屏还没拉到记录就误弹。
- **训练自评与教练点评分栏**：导入后让 AI Coach 点评时，主观感受会由 AI 压缩成短自评，教练回复也会压缩成短教练点评；如果摘要失败，自评会用本地简短兜底。点开活动后按「自评 / 教练点评」两栏展示，每栏最多两行。
- **首屏核心数据加载保护**：进入应用前会先等训练、赛事、教练消息和每日记录加载完成；开屏结束后若仍在等待，会显示核心数据加载提示，避免短暂空壳页面。
- **Codex Runner 状态刷新提速**：运行器状态刷新改为更短间隔，并收紧在线 / 不稳定 / 离线判断时间，让 AI Coach 顶部状态更快反映公司电脑 Runner 的实际状态。

## 2026-07-03

- **Memory facts 分类摘要**：Memory 页面默认按分类显示摘要段落，展开后再管理单条事实，减少每次更新后从头到尾重读全部事实的压力；底层仍保留可归档、恢复和删除的事实卡片。
- **日历当日详情面板稳定化**：日历日期底部面板按过去日期、今天和未来日期使用固定高度；关闭按钮移到右上角，底部只保留居中的前一天 / 后一天和日期信息，切换日期时位置更稳定。
- **开屏问候时间段修正**：11 点仍显示 Good morning / 早上好；12 点显示中午好，13 点后才进入下午问候。
- **日历选中日期反馈**：点开日期后，背景日历会高亮当前选中日期；在底部面板里切换前一天 / 后一天时，背景日历会同步切到对应月份并更新高亮。

## 2026-07-02

- **网页版正式入口切到裸域**：正式 Web 入口统一为 **https://ultreia.run/**；登录跳转、注册验证邮件、站点 canonical / 分享 metadata、登录页入口和分享海报底部网址都已同步，`www` 地址仅作为跳转保留。
- **移动端底部 tab 秒切优化**：底部导航在按下瞬间就切换视觉状态和页面位置，加入轻微震动反馈；已访问过的 tab 会保留挂载，剩余 tab 在空闲时间预热，减少后续切换时的等待感。

## 2026-06-30

- **Aevum 账号命名对齐**：登录、设置和删除账号相关文案统一改为 Aevum 账号；删除账号弹窗明确说明会删除整个 Aevum 账号，以及 Aevum / Ultreia / Viatica 下的个人数据，避免误解成只清空 Ultreia 训练数据。
- **删除账号链路对齐**：删除账号流程改为以 Aevum 账号为中心执行，避免先清理局部产品数据再删除账号造成半删除状态。
- **Memory 与目标赛事对齐**：手动和夜间 Memory 更新都会读取当前 Races 里的目标赛事，把赛事日期、A / B / C 优先级、距离和爬升视为结构化 source of truth；旧聊天里提到的过期优先级不会再写成长期记忆，Memory 只保留“HYROX 不应取代越野主线”这类策略边界。
- **AI Coach 输入性能优化**：聊天输入框改为本地即时响应，停止每敲一个字就重算完整上下文 token 和重渲染整段聊天历史，减少长对话下的轻微输入延迟。

## 2026-06-29

- **移动端滚动与闪动优化**：移动端关闭高成本背景 blur / ambient 动画，AI Coach 历史对话滚动时不再每帧更新跳转按钮；Memory facts 和 Races 移动端页签去掉切换动画，减少 Android WebView 上的轻微闪动。
- **日历建议表单再修复**：AI Coach 提炼到日历的建议审核里，移动端 `Time of day` 字段改为独占一行，不再挤出屏幕。
- **PWA 底部导航安全区修复**：PWA 启动背景和系统栏兜底色改为深色，同时移除会遮住 AI Coach 输入栏的额外底部背板，让底部导航只由自身背景承托。
- **Memory 导入反馈修复**：长期记忆审核卡点击导入后会立即显示导入中并禁用重复点击，避免网络写入期间看起来像按钮没反应。
- **Memory 审核减负**：长期记忆更新不再把整份快照都当成新增内容展示；审核页会区分新增、更新、建议归档和已存在事实，没变化的旧事实自动保留，只需要重点看变化项。
- **夜间记忆审核日期修复**：00:05 的后台 Memory 审核改为检查刚过去的本地日期，而不是刚过零点后的新一天，避免夜间任务因当天 00:00 后还没有对话而跳过。
- **AI Coach 自动压缩上下文**：下一次发送预计超过约 180K 上下文时，会先锁定界面并把较早对话压缩成一条摘要，只保留最近原文，减少长对话继续发送时丢上下文的风险。
- **AI Coach 训练偏好**：教练设置新增按周一到周日、上午 / 下午下拉选择的训练偏好；这些偏好会作为默认排课锚点进入 Coach 和周复盘上下文，但不会变成不可调整的硬约束。AI Coach 设置里的编辑资料入口同步移除。
- **AI Coach 训练偏好收敛**：训练偏好说明压缩成标题右侧的 `!` 提示，上午 / 下午在手机上同一行显示；下拉只保留未设置、休息、公路跑、越野跑、力量训练、HIIT、Massage，并明确进入日常对话、周复盘、赛前简报和主动日历建议。
- **AI Coach 设置菜单简化**：教练设置不再显示「教练 / 动作」灰色分组，首屏直接显示教练偏好、训练偏好、Memory、最近教练建议和高级。
- **启动和底部导航优化**：开屏动画保持约 3.6 秒；启动只等待关键资料和设置，有训练缓存时先进入 App，再后台刷新完整数据。底部 tab 在触摸按下时立即切换，减少点击后的延迟感。
- **AI 周复盘日期匹配修复**：周报现在只显示当前日期范围对应的报告；周一查看上周周期时，不会再误显示更早一周的旧周报。
- **AI Coach 收件箱重组**：每日推送和 AI 周复盘入口从 Settings 挪到 AI Coach 收件箱；收件箱改为每日推送 / 周报两个 tab，底部固定清空和设置按钮。周报不再分本周 / 上周，默认以上周日期 chip 进入，点开后直接在全屏周报里分析或导入计划。
- **AI Coach 收件箱交互修复**：每日推送和周报设置从收件箱打开时会保留收件箱背景，关闭后回到原 tab；周报详情关闭后回到周报 tab。周报列表改为每行一个周期，并固定显示本周和上周，按时间从近到远排列。每日推送 / 周报设置说明收进标题右侧 `!`。
- **AI 周复盘运行提醒**：周报生成时不再只有详情页显示动画；底部 AI Coach tab、AI Coach 顶部信封、收件箱周报 tab 和对应日期行都会显示复盘进行中的动画提示。
- **AI Coach token 预估浮窗**：顶部 Codex / Model chip 的上下文用量展开改为浮窗，不再挤压聊天区；点屏幕其它地方会直接关闭浮窗。
- **AI Coach 建议更安静**：主动日历建议不再因为一次普通漏练就自动弹出；连续偏差、主课偏差或明确恢复风险才会自动生成。近期连续跳过同类建议后，系统也会降低自动出现频率，手动一键综合调整仍可用。
- **AI Coach 建议质量信号**：最近教练建议会显示质量信号；已采纳并保存、已跳过、保存失败等结果会进入下一轮 Coach / 周报上下文，帮助教练少重复低价值建议。
- **AI Coach 当前焦点**：教练设置新增“当前焦点”入口，汇总当前目标对象、训练周期重点、计划偏差 / 恢复风险 / 赛前简报观察项，以及下一步可确认动作，不占用 AI Coach 顶栏图标。
- **AI Coach 当前焦点细化**：当前焦点拆成长期焦点和短期焦点；从焦点里打开赛前简报或一键综合调整时会叠在当前焦点上方，关闭详情后仍回到当前焦点，返回逻辑不再跳回 AI Coach 主界面。
- **AI Coach 当前焦点说明和弹窗残影修复**：当前焦点面板补充修改入口说明，长期焦点通过 Races 的目标赛调整，短期焦点通过 Calendar 的未来计划 / 完成状态调整；从焦点打开简报或一键调整时使用不透明实底详情卡，避免下层焦点内容透出来影响阅读。
- **日历当日详情底部化**：点击日历日期后从底部打开当日详情面板，上方仍能看到日历；日期、星期、前一天 / 后一天按钮固定在面板底部；点一下训练行即可打开编辑 / 删除菜单，不再需要长按；当天有目标赛事或历史赛事时会在详情页展示。
- **AI Coach 收件箱三分栏**：收件箱新增 **其他** tab，Memory 更新、赛前简报、充值 / 支付提醒等非每日推送、非周报消息会从每日推送里分出来；Memory 更新和赛前简报的教练建议记录也能从这里回看。
- **导入自评输入和展示修复**：上传活动后填写主观感受的输入框恢复为普通文本框，减少安卓九宫格拼音被当成字母直接输入的问题；训练卡片和日历详情里的「自评 / Self review」标签改为按当前语言显示，冒号后只保留自评内容。

## 2026-06-28

- **AI Coach 上下文用量提示**：AI Coach 顶部 Codex / Model chip 增加上下文小圆环，点击后只显示“已用 / 200K”和“剩余”；长对话提醒改为按估算 token 余量触发，不再固定按 40 条消息触发，并改用 OpenAI 兼容 tokenizer 提升估算精度。
- **导入备注输入法修复**：FIT / CSV 导入后给 AI Coach 写主观感受的备注框改为非受控输入，减少安卓九宫格拼音输入时被 React 重写成 WXYZ 字母串的情况。
- **导入保存防重复**：活动导入保存期间会出现“上载中”阻断提示，并禁用导入按钮，避免保存中重复点击导致重复上传。
- **周日导入弹窗排队**：同一次导入既勾选 AI Coach 点评、又触发周报提示时，先处理 Coach 点评确认，再显示周报分析提示，避免两个弹窗连续抢焦点。
- **训练负荷改为平滑 ACWR**：训练负荷条和 AI Coach 上下文不再用硬滚动 7 天总和，而是用 7 天 / 4 周 EWMA 平滑 sRPE，避免一次大越野在第 8 天突然整块掉出导致 ACWR 大起大落。
- **日历计划支持主课标记**：手动计划可以勾选“主课”，AI Coach 会把它当作本周优先保护的关键课；自动调整建议碰到主课时会先提醒，要求确认理由足够充分再保存。
- **主动调整不再回改当天计划**：计划偏差 / 恢复风险自动生成的日历建议只会处理明天及之后的计划，避免当天训练已经完成后又提示修改今天那条计划。
- **周复盘输入栏和建议表单修复**：Android App 里的 AI 周复盘底部输入栏去掉重复安全区留白，贴近 PWA 的位置；日历建议审核里的 `Time of day` 字段不再溢出。
- **导入自评更短更稳**：AI Coach 导入点评回写的「自评 / Self review」备注只从你的原始主观感受压缩生成，不再把教练点评拼进备注；训练列表和日历详情里最多显示两行。
- **Codex runner 超时诊断修复**：AI Coach 对话和手动周报的 Codex 等待窗口加长，runner 后完成时不再把已经 fallback 的任务改回 completed，方便判断是真离线还是超时回退。
- **开屏页动效调整**：合并 App 内 logo 开屏和数据加载问候页，改成约 3.6 秒的地形线、山路和最终 logo 构建动画；`Ultreia` 艺术字从实时 SVG 笔迹遮罩改为轻量文字层裁切显现，优先减少手机 WebView 上的分段感和掉帧；问候语改为 120 条人工固定池，不再用动作和场景拼出几百条组合句。

## 2026-06-27

- **训练位置删除修复**：位置列表里的删除按钮会正常弹出确认框，并显示明确的删除按钮；位置地图底部确认栏再次改为容器内独立层，减少手机 PWA / Android WebView 里被地图层盖成白块的概率。
- **地图底栏和日历建议流程继续修复**：位置地图底部确认栏改为独立固定层，避免真机上被地图层盖成白块；周复盘输入栏进一步贴近底部导航；一键综合调整弹窗改为初始只显示“生成建议”，生成中只显示 Stop，右上角关闭会收起弹窗但让生成继续在后台跑，生成完成后先出现顶部提示，点击后再进入日历建议审核。
- **地图和 Coach 输入栏细节修复**：位置地图全屏页去掉模糊浮层并降低拖动时的页面重绘频率，减少 Android WebView 闪动；位置管理页标题改为随内容滚动；AI Coach 和周复盘底部输入栏统一高度、按钮样式和位置，并去掉输入框里的灰色提示字；一键综合调整生成中也可以点 Cancel / 关闭来退出弹窗。
- **位置地图底栏稳定性修复**：全屏地图拖动时底部地址栏改为固定实色层，避免 Android WebView 出现黑色块和闪动；拖动中只实时更新经纬度，松手后再刷新具体地址。

## 2026-06-26

- **训练位置支持多点保存**：AI Coach 顶部定位入口改为“位置”面板，可打开全屏地图定位选点、备注保存多个训练地点，并在列表里选择默认天气位置。
- **位置地图交互优化**：位置管理改为全屏页面；地图拖动减少黑色闪动，提前加载周边瓦片，支持更连续的双指缩放；如果配置高德 JSAPI，会使用正式地图引擎，未配置或加载失败时自动回退到基础地图；中文地址里的繁体区县字样会统一转成简体。
- **位置地图显示修复**：基础地图兜底模式恢复稳定瓦片铺满，修复真机上只显示一小块地图的问题；底部地址和确认按钮改为上下排列，避免确认按钮在右侧形成黑色遮挡块。
- **计划位置天气请求收敛**：保存多个位置本身不会拉天气；AI Coach 只会为 7 天内计划绑定的少量训练地点补预报，并加短时缓存，避免连续对话重复请求同一地点。
- **计划可绑定训练地点**：日历手动计划可以选择已保存位置；AI Coach 计划上下文会看到计划地点，7 天内尽量按该地点补充预报；FIT 导入无 GPS 时会优先用同日同类型计划位置抓天气。
- **天气定位入口前移**：AI Coach 顶部在天气和收件箱之间新增定位图标，固定显示城市缩写；点击可进入位置面板管理训练地点，不再需要去个人资料里找经纬度。首次引导也会提示从这里设置天气地点。
- **天气定位细节修复**：定位面板改为稳定的位置预览，不再依赖外部地图加载；AI Coach 顶部缩写优先显示城市级简称，街道级地点会尽量归到所在城市，避免出现 YUA 这类奇怪缩写。
- **每日推送格式更像跑者语言**：每日教练推送会保留 HYROX 这类标准赛事写法，并把日期、距离和时长保持为 8月15日、6.2km、24h 这类数字格式。
- **长期记忆改为快照合并**：接受 Memory update 时不再把 AI 结果简单追加；应用会把新建议当作当前记忆快照，复用相近旧卡、归档被替代或过期的旧卡，只新增真正新的事实，并收紧五类记忆的分类边界。
- **长期记忆清理更完整**：Memory facts 取消 20 条显示上限；Archived 列表里的事实现在可以二次确认后永久删除，方便清理重复或过期记忆。
- **安卓常驻通知数据更清楚**：通知栏常驻统计里的 Sessions / Time / Distance / Ascent 数值字号稍微放大，同时保持原来的两行分列垂直对齐。
- **Hyrox Pro 双人组显示更短**：英文界面下赛事卡片把 Men's / Women's Doubles Pro 简写为 Doubles Pro，避免长 chip 挤占卡片宽度；底层组别仍按男双 / 女双保存。
- **AI Coach 输入栏对齐**：手机端聊天输入框修正 textarea 基线留白导致的上移，输入框和图片 / 发送按钮回到同一条底线上。
- **Version 更新内容固定中文**：Settings 里的更新内容固定中文；GitHub release 里的英文提交标题会先翻译，未知英文也会兜底成中文摘要。
- **Version 操作收紧**：Settings 里的版本行改成一行显示，按钮随界面语言切换；手动检查且没有新版本时，左侧提示已是最新版本，按钮变成查看最近更新，收起后回到检查更新。
- **AI Coach 设置返回修复**：从教练设置里点一键综合调整后，取消或关闭二次确认会回到原设置弹窗，不再直接退出到 AI Coach 页面。
- **比赛卡片移动端换行优化**：手机端赛事卡第一行只放优先级、类别、组别和倒计时，具体日期移到第二行赛事名前，避免 Hyrox 长组别把 chip 挤出屏幕。
- **AI Coach 支持图片**：聊天输入框新增图片附件，可把截图或照片发给教练；图片只用于本次 Codex 分析，不保存原图。带图请求会强制走 Codex，Codex 不可用时明确提示重试或改成文字描述，不再回退到 DeepSeek。
- **Hyrox 组别补齐**：赛事表单新增男双 Pro / 女双 Pro、男子 / 女子 / 混合接力；新增 Hyrox 赛事时会按资料里的性别隐藏另一性别组别，混双和混合接力保留。
- **安全扫描问题修复**：Android 版关闭应用备份和明文流量许可，移除未使用的旧图标资源工具，并更新构建依赖补丁版本，安全审计不再提示已知漏洞。
- **一键综合调整防误触**：手动点击一键综合调整会先二次确认，生成中可直接停止；停止后不会继续打开日历建议卡片。
- **AI Coach 点击闪动修复**：去掉全局按钮按下位移和设置类弹窗入场动画，减少点击 `!`、教练配置、长期记忆等入口时的页面闪动。
- **提示词预览切换统一**：提示词预览里的 EN / 中切换改成和 Settings 语言一致的胶囊滑块样式。
- **日历建议确认更轻**：建议确认弹窗把风险说明、确认提示和整理来源收进 `!` 说明按钮，默认只保留「教练判断」「日历影响」「准备写入」这些实质内容；日期展示增加星期。
- **AI Coach 入口前移**：底部和桌面 tab 顺序调整为 Training / Calendar / AI Coach / Races，突出 AI 教练在产品里的位置。
- **导入自评回写精简**：导入后让 AI Coach 点评时，备注回写改为生成「自评」短备注，不再直接写入完整原话。
- **AI Coach 设置入口收敛**：设置菜单改成「教练 / 动作 / 高级」三组，最近教练建议只在已有记录时显示，Prompt 预览和清空对话不再占据首屏。
- **AI 周复盘注解下调**：移除低频的正文选区注解，只保留底部追问输入框，可以把当前报告和补充问题一起发给教练讨论。
- **分享海报背景更新**：去掉 Day 模式，统一使用暗色品牌海报；背景改为放大铺满当前 logo，并压低亮度保留山路、等高线和图标轮廓。

## 2026-06-25

- **新 logo 全量替换**：应用内 logo、登录页、开屏页、PWA / favicon、Android 桌面图标、Android 开屏图和分享海报背景 mark 全部改用新的深色山路图标；同时更新了使用手册里的品牌说明。
- **分享海报背景精修**：海报背景 mark 不再硬抠金属 logo；改为保留完整金属山路质感，同时清掉原图里的等高线和外框，避免出现没抠干净的黑块或线条残留。
- **力量导入更严格**：FIT / CSV 导入里的 Strength 现在保存前必须选择训练部位；Strength 默认不抓天气，混合导入时后台也只会给户外 / 天气相关活动抓取天气。
- **导入点评避免口径打架**：导入后主动发送给 AI Coach 点评时，聊天里显示你的自然备注，后台会同时带上新增活动、当前计划和负荷信号，让 Coach 在同一轮里给出下一次计划的唯一口径；当下计划偏差 / 恢复负荷守门 / 综合调整签名会被记为已处理，避免刚点评“明早 6km easy 可保留”又立刻弹出“降到 4km”的割裂体验。
- **AI Coach 顶栏重新留白**：手机端“一键综合调整”移进教练设置的 Agent 分组，收件箱和设置按钮不再被长按钮挤在一起；AI 周复盘页面锁住外层高度，只让报告正文滚动，底部标注输入栏不再被整页拖动带偏。
- **RPE 改为必填**：手动新增 / 编辑训练时必须填写 1-10 的 RPE；导入活动也必须填写 RPE，6 条及以上用一个批量 RPE 先统一写入，之后仍可逐条修改。
- **导入自评写回备注**：导入后选择让 AI Coach 点评时，弹窗里的主观感受会随点评请求一起发送；教练点评完成后，应用会把这句话以「导入自评」追加到对应训练备注。
- **训练备注展示补齐**：手机端点开训练卡、日历点开某天的训练详情后，都会显示这条训练的备注。
- **训练数据加载更稳**：PWA 强制刷新时，训练记录加载失败不再静默进入空列表；应用会优先使用上次成功加载的本地缓存，没有缓存时显示重试页，避免误以为记录被清空。
- **数据质量助手下调**：Phase 6 不再单独推进“数据质量补全助手”，改用 RPE 必填、导入审核和备注写回这些输入约束解决。
- **主动建议自动生成**：AI Coach 上方的计划偏差 / 恢复负荷提醒不再需要先点生成；命中条件后会自动整理一份可查看的日历建议，确认前仍不改日历。
- **日历建议影响更清楚**：建议确认弹窗新增「教练判断」和「日历影响」，逐日说明是新增、替换、计划休息还是只改某条计划；未列出的日期保持原计划。
- **日历建议文案改进**：中文界面下建议分析和每条计划原因不再直接显示英文旧备注；日历影响会按当天是否还有其他计划分别说明，避免无计划冲突时仍提示“其他计划保留”。
- **主动建议冲突合并**：计划偏差和恢复风险同时出现时，会合并成一份综合调整建议，优先保护恢复 / 负荷，避免一边补漏练一边又建议降载。
- **主动建议减少打扰**：主动建议新增“3 天内不再提醒”，避免每天填写晨间状态或备注时反复弹出；AI Coach 顶部新增“一键综合调整”，需要时可以主动生成当前建议。
- **赛前简报 / 装备检查**：任意目标赛进入 14 天窗口后，AI Coach 会自动整理赛前 briefing 和 checklist，可查看并继续追问；第一版不自动改日历，支持 3 天内不再提醒。
- **天气驱动计划调整暂缓**：教练建议矩阵里将天气驱动改日历下调为暂缓项；天气预报不够稳定时不应打乱原训练计划，后续最多先作为提醒或赛前 checklist。
- **恢复负荷守门 Action Card**：AI Coach 会结合 ACWR、近期 RPE、晨间状态、训练备注和未来计划，在恢复风险较明显时提示生成一份小调整建议；确认前不改日历，且只做训练安排建议，不做诊断。
- **教练建议矩阵入口**：AI Coach 设置新增只读的「教练建议矩阵」，集中展示 Phase 6 各类建议卡片的触发条件、建议内容、边界和当前状态；矩阵由应用内清单驱动，后续推进新卡片时可同步更新。
- **Action Card 文案更柔和**：AI Coach 里的「审核建议动作 / 接受并执行 / 最近 Agent 动作」收敛为「看看日历建议 / 加入日历 / 最近教练建议」这类更像教练草案的表达；确认前仍不会改动日历或 Memory。
- **AI Coach 增加 Codex 状态点**：AI Coach 顶部模型状态前新增小圆点，绿色表示下一次预计调用 Codex，橙色表示检查中或连接不稳，红色表示离线或上次调用失败；模型 chip 会随状态显示 Codex 或 DeepSeek fallback。
- **Codex 状态刷新更快**：AI Coach 的 Codex 状态改为约 5 秒刷新一次，并按最后心跳时间在前端本地更新颜色；电脑关机或断网后通常会在约 20-30 秒内显示离线。
- **Codex 认证异常更明确**：如果本机 Codex runner 使用的 OpenAI 认证失效，状态点会保持红色并显示认证异常，AI Coach 会直接使用 DeepSeek fallback，避免每次对话都先尝试失败的 Codex。
- **Codex 路线覆盖更多 AI 入口**：AI Coach 对话、周复盘、计划提炼 / 补救、Memory 更新、每日推送、后台周报和夜间 Memory 审核都可优先调用 desktop Codex runner，失败时自动回退 DeepSeek。
- **个人模式移除钱包前置**：AI 与天气请求不再检查余额、扣钱包或提示共享余额不足；旧钱包数据和隐藏入口保留为 dormant，不影响当前个人使用。
- **AI Coach 路线显示收敛**：回复下方和模型状态里不再显示 “Desktop Codex”，统一简写为 “Codex”；生成计划偏差补救动作后，会隐藏当前这次偏差提示，避免同一批漏练反复提醒。
- **安卓常驻通知更安静**：本月统计常驻通知改用静音通道，更新统计时不再重复启动前台服务，降低日历改动、导入计划或训练数据刷新时弹出顶部浮窗的概率。
- **移动端切换更顺滑**：Training 内 Activities / Charts 手势切换不再每帧重绘整页；底部五个 tab 跨多页跳转时直接切换，避免挂载中间重页面导致掉帧。
- **AI Coach 桌面 Codex 路线 POC**：新增 desktop Codex runner 的最小链路草案和服务端 fallback 支持；桌面 Codex 在线时可优先生成 AI Coach 回复，离线 / 超时 / 失败时自动回退 DeepSeek，并在回复下方显示实际使用路线。
- **天气更新频率同步账号**：Settings 里的天气自动更新开关和频率不再只存在本机，会写入账号并在换设备后同步。
- **AI 可见输出统一中文**：AI Coach 对话、每日教练推送、AI 周复盘、日历建议和赛前简报默认给你中文内容；长期记忆仍保留中英双语，方便后续喂给教练。
- **AI Coach 顶部按钮调整**：一键综合调整从拥挤的顶栏移到教练设置的 Agent 分组，收件箱和设置按钮保留更清楚的点击空间。
- **长期记忆改为全屏页面**：Memory 不再是小弹窗；“从当前对话更新”按钮放到标题右侧，事实卡片改为 Current / Archived 筛选 + 分类 chip 的更清爽结构。
- **长期记忆列表更紧凑**：Memory 页面顶部说明和事实提示压缩成短句，“显示 20 条”移到记忆事实标题旁；分类 chip 新增“全部”，可按主题筛选事实卡片。
- **教练行为设置改为下拉列表**：教练风格、输出长度和干预强度从一排排按钮改为三个下拉选择，设置面板更紧凑。
- **开屏问候语去状态化**：问候语池清理掉与具体训练状态 / 计划绑定的句子，补充更多纯激励文案；轮换逻辑改为整池洗牌，一轮没抽完前不会重复同一句。
- **深苔藓绿动态光效增强**：移动端和桌面端外壳加入更可见但克制的 deep moss / olive ambient light，Training / Calendar / Races / AI Coach / Settings 五个移动端 tab 都能透出背景光效；active tab、按钮 hover 和输入 focus 增加低饱和 moss glow，不改业务逻辑。

## 2026-06-24

- **Memory facts 成为长期记忆主线**：接受 AI Coach 的 Memory 自动更新后，会把已保留条目拆成可查看的事实卡片，按伤病 / 健康、目标 / 比赛、训练偏好、教练风格和长期模式分类；事实可在 Memory 面板中查看、归档、恢复。
- **Memory facts 保存更可靠**：接受 Memory 更新时会先确认事实卡片已经写入账号，再把 Agent 动作标记为已执行；如果保存失败，审核卡会保留并显示错误，避免出现“动作完成但换设备看不到事实”的假完成。
- **Memory facts 时间字段补全**：保存事实卡片时会自动补齐提议时间和接受时间，修复新事实因缺少 `proposed_at` 而无法写入云端的问题。
- **旧文本 Memory 退出教练上下文**：AI Coach 和 AI 周复盘只读取 Current Memory facts；Archived facts 和旧的整段 Memory 文本不再进入 Prompt。
- **旧 Memory 清理 SQL**：新增一次性清理脚本，用于在确认迁移完成后清空账号里的旧文本 Memory 字段，不影响 Memory facts、聊天、动作记录或训练数据。
- **Memory facts 诊断 SQL**：新增只读检查脚本，可同时查看账号下的事实卡片和最近 Memory 更新动作，方便确认数据是否真正写入云端。
- **夜间记忆审核**：AI Coach 的 Memory 设置里新增夜间记忆审核开关；开启后，如果当天有新的教练对话，后台会在夜间生成待审核的 Memory Action Card，第二天打开 App 后再由用户确认启用事实，不会自动写入长期记忆。
- **移动端显示细节修复**：Training 顶部 Distance / Ascent 统计不再显示千位逗号，ACWR 负荷条保持单行；Settings 版本号移到 Version 标题后方，字体回到原来的 Outfit，并取消 Training 内 Activities / Charts 切换时的轻微抖动。
- **深色界面高级感增强**：主色压暗为深苔绿，加入柔和动态流光、微噪点、细线微光和按钮 / 输入框微交互；比赛 tag 改成暗底描边，不再使用亮色底配白字。
- **深色界面颜色修正**：主色从蓝紫调整为更贴近当前 logo 的深苔绿 / olive；Training、Races、Settings、天气设置和 AI Coach 里的选中态修复为深色可读样式，不再出现白底浅字或按钮挤压。
- **全局界面风格调整**：App shell 改为接近 Aevum 的 Linear-inspired 深色产品界面，使用暗色半透明面板、细边线和克制的深苔绿 / olive 焦点色；活动列表、导入预览、设置弹窗、删除确认和底部导航同步收敛到新风格。
- **安卓常驻通知统计口径修正**：本月 Distance 和 Training 顶部本月 Distance 保持一致，只统计 Road Run / Trail Run / Hiking / Floor Climbing 的距离，不再把 HIIT / Strength 里可能残留的距离算进去。
- **安卓常驻通知对齐优化**：通知栏本月统计改成固定宽度的两行显示，让 Sessions / Time 和 Distance / Ascent 的分隔点、右侧字段更稳定对齐。
- **安卓常驻通知列对齐修正**：常驻通知改用原生分列布局显示本月统计，让 Sessions / Distance 的数据列、两个分隔点、Time / Ascent 标签列和右侧数据列分别对齐。
- **AI 周复盘改为服务端定时**：自动周报会按设置中的本地星期和时间在服务器后台生成，即使 App 没有打开也会运行；完整报告写入账号周报页，完成后发送系统通知和收件箱提醒。
- **周日导入提示改为补漏**：本周周报生成后又导入新活动时会询问重新分析；预定时间已过但服务器仍未生成时也会提供手动兜底，时间未到则不重复打扰。
- **后台 Coach 对齐 facts-only**：服务端周报、每日教练推送和夜间 Memory 审核都改为读取 Current Memory facts，不再依赖已退出 Prompt 的旧文本 Memory。
- **Recent Agent Actions 更直观**：展开计划修改记录时按日期展示「原计划 / 新计划」，不再露出内部计划编号；执行结果改成更短的摘要标签。
- **计划偏差补救 Action Card**：AI Coach 会识别最近两周漏练 / 部分完成的计划，并可生成一张补救动作卡，建议调整接下来几天的计划；确认前不会改动日历。
- **系统弹窗收敛为应用内提示**：AI / Memory 更新错误、表单校验、放弃未保存修改、收件箱清空和 Memory facts 归档确认改为 Ultreia 自己的应用内弹窗，不再出现浏览器 / PWA 的系统级站点提示。

## 2026-06-23

- **Recent Agent Actions 可视化**：AI Coach 设置里新增“最近 Agent 动作”，可查看最近 10 条计划导入 / Memory 更新动作的类型、状态、来源、涉及日期 / 数量、失败原因，并展开查看动作内容和执行结果摘要。
- **Agent 动作弹窗更轻量**：手机端最近 Agent 动作弹窗收窄到接近 Coach settings 的尺寸，并限制高度，可在弹窗内上下滚动查看历史。
- **Action Card 支持修改单条未来计划**：当 Coach 明确调整已有计划时，审核卡会标记为“修改”，执行时只替换目标计划，不再把同一天其它计划一起清掉。
- **执行记录即时刷新**：计划导入和 Memory 更新动作在提议、接受、执行、忽略后会立即同步到“最近 Agent 动作”，不再需要刷新 PWA 才能看到。
- **执行记录展开更可读**：最近 Agent 动作展开后改为显示动作内容、执行结果和计划修改前后摘要，不再直接露出内部 JSON。
- **执行记录可以继续追问**：展开某条最近 Agent 动作后，可以带着这条动作的摘要和执行结果直接问 AI Coach，让它复盘这次动作是否合理、下一步该继续还是调整；这不会自动执行新动作。
- **执行记录支持长按删除**：最近 Agent 动作里可以长按某条 Action Card 记录删除测试记录，并同步清掉对应消息下方的已提炼状态；已写入日历的计划不自动回滚。
- **AI Coach 不再只吐计划行**：提示词增加约束，建议可导入计划前必须先给教练判断、原因和风险依据，避免回复只剩一行日历命令。
- **AI Coach 草稿不再丢失**：在 AI Coach 输入框里写到一半，切到其它 tab 再回来，未发送内容会保留。
- **AI Coach 开始读取动作反馈**：AI Coach 对话和 AI 周复盘会带上最近几条 Action Card 结果，例如已接受、已忽略、执行失败的建议，让后续建议能参考你的真实取舍。
- **Agent 动作状态更完整**：Action Card 接受后会先记录为已接受，日历保存成功后再标记为已执行并记录创建数量；保存失败会标记为失败，方便之后排查或重新提炼。
- **Agent 动作记录接入账号**：AI Coach 提炼出的计划导入和 Memory 更新动作会保存到账号下，换设备后仍能恢复已提炼 / 已执行 / 已忽略状态；确认前仍不会自动改日历或 Memory。
- **安卓常驻通知改为本月统计**：开启每日教练推送后，通知栏常驻信息改为显示本月 Sessions / Time 和 Distance / Ascent，正文尽量拆成两行方便阅读。
- **周复盘自动设置统一滚轮**：自动周报设置里的星期和时间都改成应用内滚轮选择，两个选择框宽度一致，不再一宽一窄。
- **日历计划状态简化**：计划完成度保留完成 / 部分完成 / 漏掉，不再显示单独的跳过状态；未来同一天多个计划会按上午、未指定、下午排列。
- **计划休息语义收紧**：计划休息只表示教练或你明确安排的休息日，可用于覆盖同日旧计划；普通空白日仍只是没有活动记录，不会自动打计划休息标签。
- **AI Coach 消息更清爽**：每条 AI 回复下方的模型 token / 内部成本明细先隐藏，后台成本记录不变。

## 2026-06-22

- **AI 周复盘支持多段注解**：可以选中 AI 周复盘里的多段文字，分别写注解后一次性发给教练继续讨论；发送前不会自动修改日历或 Memory。
- **注解和周报操作更贴近手机使用**：选中文本后可直接在正文附近加注解，底部输入栏固定不随正文滚动；周报的分析 / 计划按钮缩小到标题栏右侧竖排显示。
- **AI 长任务可停止**：AI Coach 聊天、AI 周复盘分析、建议动作提炼都加了停止入口；Settings 里的 AI 周复盘会在分析 / 提炼中显示转圈状态。
- **AI 周复盘自动设置**：AI 周复盘支持设置每周触发的星期和时间；到点后在 App 打开或回到前台时每周自动生成一次。周日上传活动后的“是否现在分析周报”提示也可单独开关。
- **周报底部输入栏对齐 AI Coach**：周报里的注解 / 追问输入框改成和 AI Coach 主输入框一致的高度与底部位置；标题栏按钮文案从 Plan 改为 Import plan。
- **Settings 里的 AI 周复盘收拢成二级入口**：AI 周复盘点开后再选择详情或设置；自动生成时间改用和每日教练推送一致的滚轮选择，不再用会溢出的原生时间框。
- **AI Coach 普通聊天更轻量**：普通教练回复下方去掉标注讨论按钮，日常聊天直接在底部输入框发送。
- **安卓深链准备**：Android 配置加入 `ultreia://open` 唤起入口，后续 APK 发版后可用于从外部链接打开 App。

## 2026-06-21

- **更新检查更明确**：Settings 里保留手动「检查更新」按钮；进入 Settings 会自动查一次，但短时间内复用结果，不再每次切进 Settings 都重新请求。
- **导入后 AI 点评更稳**：选择让 AI Coach 点评时，先等活动真正保存成功，再弹出发送给教练的确认框；同一批活动保存中会被暂时视为重复，降低慢网或重传导致的重复记录。
- **启动加载容错**：账号初始化和训练数据读取增加超时兜底，某一项远端数据卡住时不再长期停在开屏；调试红条避开顶部安全区，关闭按钮更容易点。
- **开屏问候轮换**：问候语会在计划、昨日训练、负荷和状态等多个命中条件里轮换，不再让「今天有计划」长期占住同一句文案。
- **安卓推送保活通知**：开启每日教练推送后，安卓通知栏会显示低优先级 Ultreia 常驻通知，提高系统保留推送连接的概率；系统电池限制仍可能影响送达。
- **AI 周复盘全屏化**：Settings 里的 AI 周复盘从弹窗改为完整页面，顶部按钮不再因「分析中」换行；生成任务提升到 App 层，离开页面或切 App 后仍会继续，完成后安卓弹系统通知。
- **AI 长任务后台更稳**：非流式 AI 调用也改走后台友好的原生网络封装；AI Coach 对话在后台完成时会弹系统通知，方便切走后回来接着看。
- **AI 周复盘交互优化**：周报改为 Markdown 正文渲染，本周 / 上周各自保留最新一份；导入计划移到分析按钮旁边，报告下方可补充回答并跳到 AI Coach 继续聊。
- **日历当日标记优化**：计划休息的日级标签改成更放松的图标和文案，标签选择区改为每行两个。
- **AI Coach 当前计划识别**：问当前日历计划时，AI Coach 会优先读取实时的 Planned Sessions 数据块，不再把旧对话里的限制误当成看不到日历。
- **安卓常驻通知改为本周统计**：开启每日教练推送后，通知栏常驻信息显示本周 Sessions / Time / Distance / Ascent。
- **开屏问候随机化**：开屏副标题不再绑定当天计划、负荷或状态，改为从激励语料库随机抽取，并尽量避开近期重复。
- **AI 周复盘账号保存**：周报从本机缓存迁到账号数据，本周 / 上周各自显示最新报告；旧设备缓存会自动迁移一次，换设备也能查看新周报。

## 2026-06-19

- **Agent 化 Phase 1 起步**：AI Coach 的计划导入改成 Action Card 审核形态，先显示建议动作、风险和确认说明，再由你逐条修改 / 放弃 / 确认后写入日历。
- **Action Card 执行预览**：AI Coach 建议动作会列出「将执行」的计划清单，并提前提示哪些日期的原有计划会被替换，避免确认前看不清影响范围。
- **Action Card 休息日和缓存状态**：同一条 AI 回复提炼过后，按钮会变成「查看已提炼动作」；明确的休息日会作为「计划休息」写入日历，并覆盖同日旧计划训练；风险等级改为按影响范围显示低 / 中风险说明。
- **Action Card 状态流**：接受并执行后，AI Coach 消息下方会显示「动作已执行」；点「忽略」会显示「动作已忽略」；右上角关闭只暂时关窗，不改变动作状态。
- **Memory 更新接入 Action Card**：AI Coach 自动更新长期记忆时会先显示 Agent 动作审核卡；确认前不会写入 Memory，接受后标记为已执行，放弃后标记为已忽略。
- **Agent 化 Phase 2 起步**：新增 Settings → AI 周复盘入口，可手动分析本周 / 上周数据，生成更详细的周报页面；报告会逐条点评训练、给出接下来 7 天计划，并可继续提炼成 Action Card 后导入 Calendar。
- **个人模式入口收敛**：短期不公开给其他人使用，隐藏登录页注册入口、邀请码管理、钱包入口、充值入口和管理员充值入口；底层 AI / 天气成本记录保留，后续公开时可再恢复入口。
- **使用手册同步个人模式**：AI Coach、天气和总览文档改为说明内置 DeepSeek / 天气服务与内部成本，不再引导用户充值或找管理员拿邀请码。
- **AI Coach 等待状态优化**：发送消息后先显示「教练思考中」，只有真的收到回复正文后才出现教练气泡，避免空白小框闪出。
- **更新日志兜底修复**：最近版本的更新说明补齐中文映射；后续即使有未映射的提交说明，也不会再被过滤成空日志。
- **导入后处理弹窗**：上传 FIT / ZIP / CSV 后，审核列表不再混入天气和 RPE；点导入后统一在弹窗里处理天气、RPE 和 AI Coach 点评。1-5 条可逐条翻页填写，6 条及以上走快速批量导入并只让教练看最近 3 条。
- **开屏问候状态化**：问候语扩展为按日期轮换，并优先结合今日计划、最近训练、比赛、训练负荷和晨间状态显示更贴近当前状态的问候。
- **设置页个人模式简化**：当前个人模式下不再显示 Settings 的「其他」分组 tab，直接展示每日推送、天气自动更新、语言、使用手册和检查更新等个人设置；公开模式差异同步写入项目规则文档。
- **长期记忆按主题整理**：AI Coach 自动更新 Memory 时会按伤病 / 健康、目标 / 比赛、训练偏好、教练风格和长期模式分区；审核时分区标题默认保留、不再需要勾选，空分区会显示「无」。

## 2026-06-18

- **AI Coach 改为 DeepSeek only**：取消 DeepSeek / Claude 模型切换，AI Coach 对话、Memory 自动更新、导入日历提炼和每日教练推送都统一使用 DeepSeek。
- **AI 扣费取消加价倍率**：AI Coach 和每日教练推送改为按 DeepSeek 实际模型成本扣费，四舍五入到分，每次成功回复最低 ¥0.01，不再乘以 1.2。
- **天气自动更新频率可调**：Settings 里新增 Weather updates，默认开启，可选每 3 / 6 / 12 小时或每天一次；自动更新按上次成功更新时间滚动计算，只在打开 App 或回到前台时检查，不做后台静默更新。

## 2026-06-17

- **充值提醒系统通知修复**：安卓 App 新增钱包提醒专用高优先级通知渠道；管理员充值提醒会走这个新渠道推送，并并行发送到管理员设备，避免旧每日教练通知渠道被系统静音 / 降级后导致收件箱有记录但手机不弹系统通知。
- **充值提醒诊断更明确**：用户发送充值提醒后，如果管理员收件箱已写入但系统通知没有发出，会提示是管理员设备未注册通知还是推送发送失败，避免误以为提醒完全丢失。
- **AI Coach 流式回复恢复**：AI Coach 主对话重新变成边生成边显示，不再等整段回复完成后一次性出现。
- **管理员充值提醒修复**：钱包充值窗口扩大提醒查找范围，并兼容更多充值提醒格式，避免系统推送收到了但管理员列表里没有可点记录。

## 2026-06-16

- **AI 扣费明细更透明**：AI Coach 回复下方会显示缓存命中 / 普通输入 / 输出 token 拆分；服务端补齐 Claude 缓存写入 / 读取 token 的计费，避免缓存字段漏算。
- **钱包天气说明补充**：计费说明里补充天气抓取频率，说明缓存、手动刷新、训练天气快照和长训练多点采样分别什么时候会扣 ¥0.01。
- **新手提示补充**：注册邮箱验证、AI Coach 模型选择、钱包充值人工确认和单次海报天气状态增加更明确的提示；首次资料页仍保持一次填完整。
- **开屏与海报细节**：开屏产品名改为和海报底部一致的手写签名样式；单次海报的天气行略微上移，2 小时及以上训练如果保存了多点天气，会在海报里显示温度 / 体感温度范围。
- **新手引导更新**：首次引导改成钱包扣费口径，不再提示用户填写 API key；训练导入说明补充 FIT / ZIP / Garmin CSV，并更新支持的活动类型。
- **检查更新中文化**：更新弹窗改成中文可读列表，自动过滤内部发版词和版本号文案，Markdown 标题不再原样露出。

## 2026-06-15

- **钱包上线**：AI Coach、AI 导入日历、Memory 自动更新和天气刷新改为从钱包余额扣费；普通用户不再需要自己申请或填写第三方服务密钥，余额不足时只引导充值，并可在钱包里查看流水。
- **Claude 接入钱包扣费**：AI Coach 左上角可在 DeepSeek / Claude 间切换，聊天、Memory 自动更新和导入日历提炼都会按当前模型调用；钱包流水会显示扣费来自哪个模型。
- **钱包计费口径调整**：AI 扣费改为按模型实际 token 成本折算人民币后 ×1.2，四舍五入到分，最低 ¥0.01；天气成功获取一次固定扣 ¥0.01。
- **每日推送扣费校准**：每日教练推送不再固定扣 ¥0.10，改为和 AI Coach 对话一致，按模型返回的实际 token 用量计费；DeepSeek 缓存命中的输入 token 也按更低单价计算。
- **钱包充值流程**：钱包余额卡加入 **!** 计费说明按钮，首次打开钱包默认展开，收起后下次不再自动显示；充值按钮会显示收款二维码，用户支付后填写金额提醒管理员，管理员可在钱包充值窗口看到待处理提醒并一键填入邮箱和金额。
- **充值通知闭环**：用户发送充值提醒后会看到明确的待处理提示；管理员确认充值后，用户会收到 App 内消息，安卓 App 开启通知时也会收到系统推送。
- **充值提醒收件箱修复**：管理员收到的充值提醒在 App 收件箱里改为正常文案，不再显示内部 JSON；安卓设备切换账号后也会按当前账号重新登记推送 token，减少系统通知和 App 内收件箱不一致。
- **长训练天气范围显示**：2 小时及以上训练如果保存到了训练过程中的多点天气，展开训练卡片时会显示温度 / 体感温度范围，不再只显示起点天气；超长训练如果起点已超过 24 小时，但结束段仍在最近 24 小时内，也会尽量抓这段可查到的天气范围。
- **日历补计划**：点开任意日期都能添加计划，包括今天和历史日期；补历史计划、或下午补今天上午计划时会先提示这是补录计划。
- **AI Coach 时间边界更清楚**：今天和未来的计划会明确标成「已安排但未完成」，避免教练把今晚还没发生的训练当成已经做过；导入日历再次点击同一条回复会直接打开上次提炼结果，并可手动重新提炼。
- **导入日历弹窗更紧凑**：日期改为短显示，时段列变窄，类型列留出更多空间；力量计划只保留训练部位，去掉细分时长输入。
- **单次海报细节**：勾选天气时，上方标题 / 距离 / 轨迹区整体略微上移，给天气、签名和网址留更多空间；9:16 右上角 logo 放大到与其它比例更一致。

## 2026-06-14

- **分享海报大改**：背景换成 logo 的山峰标志（粗线条双峰 + 两个点 + 绿色横杠），按日/夜配色（绿色不变）；新增「历史」类型，把全部训练汇总成一张总览；单次海报左上角改成这条训练的真实类型、时间显示到分钟，并新增一排可勾选的数据标签（时间/配速/爬升/心率/天气，可隐藏心率等、可加天气）。
- **海报签名换字体**：底部手写体 Ultreia 签名改用更具签名感的字体，作为产品符号。
- **海报标题改首字母大写**：大标题和副标题（如 Trail Run、Weekly Volume、June 2026）改成首字母大写，数据标签仍保持全大写。
- **AI Coach 顶部更顺手**：左上角模型 pill 可点击，弹窗里直接切换模型（DeepSeek / Claude）；天气 pill 点击会跳到日历并让今天的天气卡闪一下。
- **收件箱已读逻辑优化**：消息在屏幕上完整显示后即自动标为已读（多条未读需逐条滑到完整显示），去掉「全部已读」按钮，「清空」移到底部。
- **开屏页**：第一屏改为只显示 logo 和产品名；登录后带名字的问候保留。
- **日历计划状态修复**：把已完成的计划标记为跳过后再撤销，不会再先闪一下旧状态。
- **邀请码前缀**：新生成的邀请码改为 UL- 开头（旧邀请码仍可用）。
- **产品理念补充**：使用手册说明了 Ultreïa 名字上那两个点（分音符），以及它如何成为 logo 山顶的两个点。

## 2026-06-12

- **启动修复**：修复新版安卓 App 打开时直接闪退的问题。
- **分享海报背景跟随新 logo**：分享海报背景不再引用旧的线稿资源，改为复用最终版 Ultreia logo，避免海报和应用图标不一致。
- **产品理念并入总览**：使用手册总览开头加入 Ultreia 名字由来、logo 设计思路和这款工具的使用判断原则。
- **安卓桌面图标裁切修正**：Android 自适应图标给山形 logo 留出安全区，避免桌面图标被 launcher 圆角或缩放裁掉边缘。
- **新手引导加入名字由来**：第一次打开的新手引导先用弹窗解释 Ultreia 的含义，再继续原来的训练、日历、比赛、AI 教练和设置 5 步介绍。
- **Ultreia logo 定稿**：启用最终版深色山系图标，应用图标、开屏图和分享海报同步更新，避免启动时看到两套图标。
- **产品更名为 Ultreia**：名字取自中世纪圣地亚哥朝圣之路上朝圣者相遇时互道的口号 Ultreïa——「向更远处去！」。应用标题、开屏、登录页、分享海报签名、推送通知、安装名等用户可见的地方全部改用新名字。logo 后续会跟随新名字打磨更新。
- **网页版新地址 ultreia.run**：网页版正式搬到 **https://ultreia.run/**，登录跳转、注册验证邮件、分享海报底部的网址都已指向新地址。

## 2026-06-11

- **AI Coach 活动点评改为确认后直发**：手动添加或导入活动后会直接弹确认框；确认后自动把这次活动发给 AI Coach 并打开回复页，不再跳过去预填输入框，避免手机键盘把 AI Coach 页面顶乱。

## 2026-06-10

- **横滑性能与更新日期修正**：底部主 tab 的横滑拖动更顺，降低快速滑动时的掉帧感；更新日志把 6 月 10 日上午的 logo / 海报条目移回 2026-06-10。
- **图表、海报与删除账号前备份调整**：距离趋势图下方的选中日期和数据改为靠近并居中显示；分享海报去掉大数字下方那条无数据含义的深苔绿横杠，1:1 背景三角线进一步上移，避免压住柱状图；备份导出入口改到删除账号弹窗内，点击小按钮后先展示用途说明并二次确认。
- **体能趋势、备份入口与海报细节修正**：体能 / 疲劳趋势图的纵轴改为只按 Fitness / Fatigue 曲线缩放，不再被单日训练负荷峰值拉高；状态数值恢复居中显示；数据备份入口移到账号下拉里，说明文字可换行，并在生成后提示去浏览器下载记录或系统下载文件夹查看；1:1 和 4:5 分享海报的背景三角线与深苔绿横杠整体上移，避免压到数据图。
- **数据备份、训练负荷趋势与 AI 成本可见化**：Settings 里新增数据备份导出；Charts 新增近 56 天 Fitness / Fatigue 简化 CTL / ATL 曲线；AI Coach 回复下方显示本次实际 token 数和折算成本。
- **移动端切换动画优化**：底部主 tab 点击切换恢复横向滑动动画，左右滑动的触发距离和速度阈值更低；Training 的 Activities / Charts 与 Races 的 Races / PR 在手指拖动时会跟随位移，松手后再吸附到目标页；设置页账号下拉展开/收起速度放慢，不再像瞬间闪出。

- **分享海报导出修复**：打开海报后立刻点「保存 PNG」不再可能导出缺 logo、缺背景线条的图片——导出前会先确保所有图片就绪，加载失败会明确提示保存失败，而不是悄悄给一张缺图的海报。
- **海报资源按需加载**：背景线条图只加载当前选中的风格（日间 / 夜间），切换风格时才加载另一张，打开弹窗更省流量。
- **logo 资源清理**：删除不再使用的旧 logo 图片，减小应用体积。
- **logo 资源轻量化与海报背景修正**：开屏、登录页、设置页和海报右上角 logo 改用更轻量的版本，减少首次打开时的卡顿；分享海报背景改用真实的产品三角线条图，Day 版本将白色三角转为深色并保留深苔绿横线。
- **产品 logo 统一**：应用内可见的产品 logo 和分享海报统一改用原版产品 logo，保留右下角深苔绿横线；分享海报右上角 logo 移到更靠右上，顶部横线不再穿过 logo；底部签名和网址改为居中；Day / Night 背景三角形与深苔绿横线使用同一套位置和大小。

## 2026-06-09

- **分享海报 logo 细节修正**：Day 背景不再使用带底色方块的图片，只保留放大的产品三角线条；右上角产品 logo 改为上边距和右边距一致，并继续放大；夜间模式的绿色强调色改为更接近产品图标的小横杠颜色，降低荧光感。

- **分享海报视觉调整**：海报背景改为使用产品图像纹理并叠加暗色遮罩，标题、日期、右上角 logo、关键指标和网址字号整体放大，提高保存后图片的可读性。
- **分享海报日夜模式区分**：Day 改为浅色背景和深色 logo，Night 保持深色图像质感；9:16 竖屏比例下背景 logo 不再被裁切，右上角产品 logo 进一步放大，夜间模式下 logo 右下角小横杠更清晰。
- **分享海报 logo 修正**：右上角产品 logo 始终使用原始深色底版本，不再跟随 Day 背景反相；删除额外补上的独立横杠，并继续放大右上角 logo。
- **移动端细节修复**：日历不再显示已退役的每日标记（例如旧的睡眠差标记）；开屏问候语改为按当前登录账号读取姓名，避免切换账号后沿用上一个账号；设置页账号展开、分组选项和底部主 tab 横滑切换更轻、更跟手。
- **邮箱验证与找回密码**：邀请注册后需要先完成邮箱验证再登录；登录页新增忘记密码弹窗，密码重置邮件打开后可直接设置新密码。
- **登录页入口调整**：登录页只保留登录表单，记住账号下方改为左侧「Create account」、右侧「Forgot password?」，分别打开注册和找回密码弹窗；不再在登录页显示重发验证邮件入口。
- **登录账号记忆**：登录页新增「在当前设备记住这个账号和密码」。同一设备上成功登录过的账号会出现在「已保存账号」下拉里，点选后自动填入邮箱和密码，方便切换测试账号。
- **登录页细节调整**：顶部改为居中显示产品 logo；登录错误改成更直白的「邮箱或密码错误」；底部新增「访问网页版」链接；语言切换胶囊文字改为上下居中。
- **Training 刷新方式调整**：取消下拉刷新；手机端双击底部「训练」tab 时，若列表不在顶部会先回顶，已在顶部再触发数据同步，避免翻历史记录时误触刷新。
- **设置页重新分组**：原来点邮箱展开的一长条，拆成「账号 / 管理 / 其他」几个分组，一次展开一个。点上方邮箱看账号相关（资料、改密码、退出、注销）；其他放每日推送、语言、使用指南、版本。进入设置页默认展开「其他」。

## 2026-06-08

- **顶部 5 个 tab 改为跟手翻页**：横滑时当前页跟手移动、相邻 tab 露出一半，松手吸附到目标 tab；点底部导航仍是直接跳转。Training 的 Activities / Charts、Races 的 Races / PR 这两个内部切换保留各自滑动，滑到边界后再接力翻到相邻 tab。
- **计划表单按运动类型显示字段**：加 / 改计划时只显示相关字段（时段所有类型都有）——路跑 = 跑步类型 + 距离；越野 / 徒步 = 距离 + 爬升；爬楼 = 爬升；骑行 = 距离 + 速度；游泳 = 时长；力量 = 部位；HIIT = 只有时段。AI 导入弹窗同步。
- **计划完成改为按类型 + 量化判定**：过去（和今天已完成）的计划，按当天同类型实际训练对照计划目标——达到 80% 算完成、不到算部分完成、当天无同类型算漏掉；仍可手动标完成 / 跳过。教练看到的计划依从也用同一套。
- **计划时长不再显示秒**，只到分钟 / 小时。
- **日历今日格子计数修正**：今天若已有完成训练，当天计划也收进「+N 计划」小标（之前会错显成 +1）。
- **教练点评草稿修复**：发送后再切走切回 AI Coach，输入框不再重新弹出同一段预填点评。
- **下拉刷新更稳**：往上翻看记录再滑回顶部继续下滑，不再误触发同步；切换 tab 时顶部不再闪现「下拉刷新」字样。
- **移动端切换动画与日历提示**：Training 内部 Activities / Charts、Races 内部 Races / PR 和 Target / History 切换改为轻量滑入动画；手机日历也会提示历史计划已收进当日详情。

- **晨间状态弹窗**：每天本地时间 5 点后首次打开应用，如果当天还没填睡眠 / 腿感 / 精力，会自动弹出今日状态；保存或跳过后当天不再打扰。
- **新增活动后可让教练点评**：手动添加或导入活动后，训练页会出现“让教练看”入口；最多取 3 条新活动预填 AI Coach 消息，由用户确认发送，不自动消耗 AI 调用。
- **AI Coach 滚动细节优化**：聊天窗口减少不必要的强制贴底，移动端滚动区域增加隔离，降低慢速滑动时的卡顿感。

- **导入体验修复**：审核行里的跑步分类更完整；取消导入会真正清掉待导入提示。
- **训练 / 比赛页横滑**：训练页可在 Activities / Charts 间左右滑，比赛页可在 Races / PR 间左右滑。
- **日历计划更干净**：历史日期的计划不再直接占月历格子，只在点开当天详情时显示；今天和未来计划仍会在月历里露出。
- **当日标记精简**：去掉 Travel，保留按摩 / 拉伸 / 生病，教练每日推送不再参考旅行标记。
- **AI 计划导入整理**：教练输出和导入弹窗都按更固定的计划格式处理，越野保留距离 + 爬升，力量按部位和时长拆开。
- **移动端细节**：设置页账号展开加入过渡动画，计划下拉不再被弹窗裁切，计划 AM / PM 往返保存更稳定，下拉刷新减少中途接管导致的卡顿。

## 2026-06-06

- **教练回复流式输出**：教练边想边逐字往外蹦，不用干等一整段。
- **图表新增「近 7 天」**：按天显示最近 7 天的数据。
- **导入审核界面重排**：每行第一行日期 / 类型 / 配速、第二行数据 + 末尾一个小 RPE 框，**导入时就能直接填 RPE**，不用导完再补。
- **训练负荷条挪到训练记录列表顶部**：跟统计、操作按钮一起固定不滑走；填了 RPE / 导入后立即刷新；标注更清楚（近 7 天 · 周均）。
- **海报保存提速**：字体子集化 + 去掉重复编码，存图更快。
- **去掉「睡眠差」当日标记**：已被「今日状态」的睡眠自评取代。
- 应用内使用手册的章节顺序对齐成最新顺序。
- **训练负荷 / 骤增风险**：训练记录列表顶部的训练负荷条，用近 7 天 vs 近 4 周的「时长 × RPE」算 ACWR，颜色提示加得是否太猛；也发给 AI 教练。
- **计划复盘闭环**：过期计划在月历上自动淡出、不用再手动删；点开那天能看到每条计划是完成 / 漏掉 / 跳过，漏的可一键「已完成 / 跳过」。AI 教练会看到最近两周的计划依从，漏了会先问原因。
- **晨间状态自评**：日历里给睡眠 / 腿感 / 精力打个分（差 / 一般 / 好），喂给教练判断今天该顶还是该收。
- **教练更懂计划**：未来约 21 天的计划都会发给教练（不再只发一周、也不再依赖天气），可以直接让它评估计划可行性；导入计划改成**覆盖**当天原计划而不是叠加。
- **周期 / 热适应提醒**：临近目标赛时教练会按堆量 / 巅峰 / 减量阶段说话；比赛日高温高湿会主动给热适应建议。
- **教练消息可复制**：长按教练的回复可以选中、复制文字了。
- **分享海报重做**：换掉原来一排挤在一起、长得差不多的模板，整张海报重新设计成克制的运动杂志风——最大的数字（距离 / PR 数）当绝对主角，配一条主线（单次用真实 GPS 轨迹、周/月/年用「跑量节奏条」、PR 用干净的成绩清单），底部是手写的产品签名。选项全部收成下拉框：海报类型、对象（训练 / 时间段 / PR 范围）、风格（日间 / 夜间）、尺寸（竖版 4:5 / 方形 1:1 / 竖屏 9:16）。

## 2026-06-05

- **月跑量 / 分享海报**：Charts 顶部新增海报入口，可预览并保存分享图。支持周 / 月 / 年 / PR / 单次训练五种类型，可前后切换时间段；PR 海报可按全部 / 今年 / 去年 / 近 12 个月筛选；单次训练若带 GPS 轨迹会画出克制的路线图。模板保留 Classic（训练热力图风格）和 Bib（暗色赛事海报风格），4:5 比例适合分享，安卓可直接保存 PNG 到相册。
- **移动端界面打磨**：底部导航改成更清楚的选中胶囊；Training 顶部把筛选、Activities / Charts 切换、时间范围、统计、上传 / 排序合成一个固定区，滑动时只有下方列表滚动，统计卡两行等权展示（次数 / 时间 / 距离 / 爬升）；Calendar 恢复 7 天天气纵向卡片；Settings 顶部账号卡片可展开、收纳 Profile / API / Account 入口；AI Coach 顶部状态区精简，设置和收件箱入口移到右上角，输入框最多自动增高到 7 行。

## 2026-06-04

- **上传 `.fit` 也能抓天气**：导入时若含最近 24 小时内的活动，审核面板出现「获取天气」开关（默认开），用 FIT 自带 GPS 抓天气，更早的活动跳过。手动添加的「获取天气」开关也只在训练在最近 24 小时内（或未来）时才显示，超过就隐藏，不会把"现在的天气"错贴到老训练上；天气标记改成"只要存了天气就显示"、不再限户外类型。
- **修手机上选不中 `.fit`**：文件选择器去掉类型过滤（安卓会把 `.fit` 置灰），选完仍按扩展名校验。
- **下拉刷新**：Training 页顶部下拉即可强制重新拉数据（平时只在启动时拉一次）；加了「正在同步」文字 + 动画、最短显示时间和 GPU 合成，刷新指示不再被顶栏挡住、过程更顺；双击底部「训练」图标回到顶部。
- **更新下载优化**：安装包改用安卓系统下载器——息屏 / 切后台也继续下、通知栏显示进度、下完自动唤起安装；优先走更快的镜像源，失败回退 GitHub / 浏览器。
- **性能**：活动列表一次最多渲染 60 条、底部「加载更多」按需展开；列表加载不再拉每条的 GPS 轨迹。记录上千条时切到 Training、下拉都明显不卡。
- **开屏问候**：启动页按本地时间显示「早上好，你的名字」+ 随机一句运动小话（共 68 句）。
- 每日推送加固语言指令，不再因训练备注里有中文而串成中文；修复多选计数（批量删除后继续选不再累加已删数）；路跑展开指标改左对齐；无额外数据的卡片不再展开空白；排序下拉宽高对齐；去掉时长筛选按钮。

## 2026-06-03

- **新增 Cycling（骑行）/ Swimming（游泳）**：方便练铁三。骑行看速度，游泳看每 100 米配速、用米录入；都不按心率分配速，归在「跑步 / 耐力」组但不计入跑量；导入能自动识别。
- **邀请制注册上线**：登录页点「Register」填邮箱 + 两次密码 + 邀请码即可；管理员账号菜单里能「生成邀请码」（一次性，用过会标已使用、显示注册邮箱、可删）。也能自己「删除账号」（输两次密码确认，永久不可恢复）。
- **新账号可直接体验**：不用先配置第三方服务也能试用 AI 教练和天气；天气同时降频（实时缓存 1→3 小时、去掉每小时自动刷新）让体验额度更耐用。
- **支持上传 `.fit`**（佳明 / 高驰等手表原生格式，高驰只有这个）：跟 CSV 一样自动填表，也支持 `.zip` 批量、带解析进度条。FIT 数据更全——额外算各心率区间停留时间和路线、步频正确换算、没填阈值时按年龄估算区间；图表新增「心率区间时长分布」，AI 教练也能看 Z1–Z5 占比判断"轻松跑"是否真轻松。
- **导入「未知类型」按原始名分组**：批量导入整个文件夹时每种类型只需选一次（显示条数），新增「跳过」选项；高驰「cardio」默认归 HIIT。
- **筛选 / 操作整理**：类型筛选改两级菜单（先大类再二级）；新增「时长筛选」（收进时钟图标）；Activities 操作改纯图标、上传移到最左加重（手表文件导入是现在主力）。
- **去掉 GAP 和有氧 TE 两字段**：参考意义不大，从表单 / 导入 / AI 提示词移除（旧数据不动）。
- **Claude 升级到 Opus 4.8**；Provider 标签去掉「(第三方)」后缀。
- **新手引导**：第一次填完资料后自动走一遍每个页面；设置 → 使用手册里可「重看新手引导」；登录 / 注册页右上角能先切中 / EN，选的语言注册后带进设置。
- **Hyrox 赛事加官方组别**（公开 / 精英 / 双人 / 接力等）。
- **AI 教练细节**：首次发消息给一次性温和引导（没设风格 / 记录太少 / 没目标赛事时提示并带「去设置」）；提示词里历史赛事只发每项目「最近一场 + 最好成绩」并精简重复；长期记忆手动改中 / 英任一都会真正发给 AI；设置子项关闭回到教练设置页。
- **检查更新**：进设置自动检查，有新版带红点、没有则变「查看最近更新」可看内容。
- 修复：排序按钮 / 添加训练「类型」下拉点了没反应；天气配置状态显示更清楚；AI 价格对比收进标题旁「!」图标。

## 2026-06-02

- **日历改成一个居中弹窗**（背景模糊）：点某天打开，日期居中、两侧 ‹ › 翻上一天 / 下一天，天气分两行；长按里面的活动可改 / 删，空白日点「+ 添加」加计划——看 / 改 / 加一个窗口搞定。AI 教练设置也改成同款居中弹窗。
- **应用内下拉统一**：新增支持多选的应用内下拉控件替换原生下拉。图表周期、个人资料（性别 / 职业 / 跑龄 / 心率算法，及多选的比赛类型 / 伤病 / 器材）、训练和日历表单的类型 / 时段都换成它；图表周期还会记住你的选择，不再重开变回默认 4 周。
- **聊天 / 总结记忆切到别的 app 不再报网络错**：请求进行时申请一段后台执行时间，让它在后台跑完。
- 日历当天训练按时段排序（上午在上）；路跑标记为比赛后不再要求选配速类型；PR 类别标题放大、「其余完赛」单行显示、越野赛 / 斯巴达按比赛日期从近到远排。

## 2026-06-01

- **AI 教练聊天窗口固定居中**：顶部 Provider 栏和底部输入框不再跟消息滚动，进页面默认停在最新一条；往上 / 往下翻会冒出「回到顶部 / 回到最新」按钮（滑动时才出现、停手 1 秒消失，不挡阅读）；慢速拖动不再卡。聊天气泡归到设计体系（你的消息深色"印章块"、教练浅色面板），新消息淡入。修好了手机端窗口没固定的问题。
- **教练更听你临场说的话**：对话里直接说当下变更（"今晚训练""长距离不跑了""周六挪周日"）时以你刚说的为准，不再死守日历旧计划。
- **计划训练能编辑了**：日历点开某天，计划那行右侧有「编辑」按钮，改类型 / 距离 / 时长 / 时段 / 力量部位直接存；多余的"计划"小标去掉（虚线框已够区分）。日历操作统一改成长按弹出「编辑 / 删除」弹窗。
- **检查更新**：下载 / 查看按钮挪到更新说明上方、整页顺畅下滑就能点到；更新说明只列本次变化，去掉"安装来源""Full Changelog"等样板话。
- 修复滑不动的 bug（之前为固定聊天窗口误把"撑满一屏"加到所有页面，现在只作用于 AI 教练页）；各页面底部留白收窄到刚好盖住导航条；每日推送会参考你最近在教练里说的话。

## 2026-05-31

- **左右横滑切页有动画了**：内容跟着滑的方向滑入，不再"啪"地闪过去（本就能横滑的宽表不受影响）。
- **赛事天气可折叠 + 历史赛也有天气**：赛事卡天气默认收起，点一下卡片才展开，列表更紧凑；历史赛能看比赛当天的真实天气（可查几十年前任意一天，滞后约 5 天），3 小时以上的长赛显示当天温度范围。
- **比赛标识更醒目**：训练里的比赛场显示带底色 🏆 小标（去掉撞色的绿底）；日历比赛日的日期右边也有 🏆，跟赛事列表同步。
- **日历排计划更清楚**：加了时段（上午 / 下午 / 不限），力量能选上肢 / 下肢 / 核心，日历格和详情会显示"上午 · 核心"这类信息；AI 教练导入日历不再强行每天排训练（休息日空着，识别教练说的上午 / 下午和力量部位并可在确认弹窗里调）。
- **个人资料**：先弹只读预览卡（只列已填信息），想改再点「编辑」；编辑页单选项改下拉菜单，整页短很多。
- **弹窗体验**：个人资料 / API / 添加赛事 / 教练设置等以前铺满整屏，现在都是居中浮层卡片 + 背景模糊 + 轻微放大入场动画；收件箱弹窗缩小约三分之一。
- **AI 教练**：聊天超 40 条才提示精简（之前 20 条偏早），提示里加「清空聊天」按钮（清空前提醒先更新记忆）；更新记忆可以离开界面后台继续跑、跑完顶部弹通知；点「编辑资料」会跳设置页并让"个人资料"闪一下。
- **设置 & 其他**：语言切换改中 / EN 椭圆开关；每日推送点保存立即生效；退出登录有转圈动画；使用手册目录改成框下内联下拉。
- 修复：设了默认位置就直接用、不再弹系统"位置精确度"框；更新安装失败自动重试一次；补上安卓 13+ 通知权限声明。

## 2026-05-30

- **赛事当地天气**：添加 / 编辑赛事可填比赛地点（输城市名选一个，Hyrox 室内不显示）。填了地点的目标赛事会显示比赛当天天气——临近（约两周内）显示真实预报，还早（几个月后）显示该地点该日期的往年同期典型天气，方便提前定补给装备。AI 教练也会看到最近一场目标赛事的比赛日天气，据此给配速 / 补水 / 装备建议、提示高温高湿风险（只带最近那一场）。赛事天气切过去秒显（做了缓存）。
- **长训练（≥3 小时）记录全程天气**：越野超马这类只记起跑温度意义不大，现在采样全程多个时间点（起跑 / 每约 2 小时 / 结束）喂给教练，卡上仍只显示起跑天气。
- **出差 / 旅行可填目的地**：日历勾"出差"后多一个输入框填去哪，会喂给教练（当地跑步建议、考虑气候时差）、每日推送也会引用。当日标记可标在未来日期、排成两行（"睡眠差(昨晚)"单独一行）。
- **收件箱秒开 + 未读及时清零**：启动时就加载好、点信封立即显示，已读后红色未读数马上消失；加了「去设置每日推送」按钮（跳设置并闪一下）。
- **目标赛事到比赛日一键录成绩转历史**：到比赛日赛事卡出现「录入成绩」按钮，填完确认就自动从目标移到历史。
- **记忆更新改成逐条勾选**：建议内容分条列出、每条带勾选框（新增标 NEW），取消不想要的、只保存勾上的；记忆支持中英双语（EN / 中 切换，发给 AI 固定英文版）。
- **Prompt 预览改成只显示"架构"**：核心指令隐藏，调用的你的数据只列占位符 + 一句说明，让你知道发了哪些类别但不暴露具体内容（实际发给 AI 的不变）。
- **每日推送支持半点**：时间从只能整点扩到每半小时一档，下拉选择，一天最多 3 个（仅安卓 APK 实际推送）。
- **训练 / 赛事记录改成长按操作**：短按展开、长按弹「编辑 / 删除」菜单，编辑统一改弹窗；卡片右上角删除键去掉。
- **检查更新**：弹窗只显示本次更新内容；下载安装失败会显示原因（不再默默跳浏览器）。
- 排版微调：PR 卡 ITRA 分挪到右下角不再遮成绩、各 PR 卡高度一致；训练卡 RPE 移到行尾、天气单独换行、展开更紧凑；力量子项显示 Lower / Upper；目标赛事倒计时改"还有 X 个月 Y 天"。
- 修复：切 App 回来不再频繁弹"位置精确度"框（天气新鲜就用缓存、需重拉用低精度）；补通知渠道修每日推送收不到手机通知；关闭长按误弹的"复制 / 全选"系统菜单。

## 2026-05-29

一整天密集迭代，连发多个版本（v0.3.0–v0.3.3）。

- **AI 教练看得到"周训练量趋势"**：每次发消息多带最近 8 周的周训练量（跑步总距离 / 爬升 / 次数），能识别"这周比上周猛涨 40%，注意伤病"这类风险。后台切换不再失败（走系统原生网络层，等回复时切走再回来不报"network request failed"）；Prompt 改成全量 7 天天气（未来 7 天天气一次性全发，让它按天气挑日子）；设置重新归类（编辑资料 / 教练配置 / 长期记忆归到 Prompt 组，日历导入 / 清空归到对话组），手机端教练设置改底部弹出菜单。
- **每条训练加 RPE + 备注（可选）**：RPE（自觉用力 1–10）帮估算负荷、备注当背景，所有类型可填；折叠卡也显示 RPE，天气温度去小数。
- **每日教练推送（APK）**：可设最多 3 个推送时间，服务端按时区到点生成简短 AI 打卡（语言跟随 App）。推送存进应用内「收件箱」（AI Coach 页右上角信封图标），系统通知划掉也能回看，支持标已读 / 删除 / 清空、未读带红数。
- **日历当日标记扩展**：从只有"按摩"扩到按摩 / 拉伸 / 睡眠差 / 生病 / 出差（小图标可多选），也喂给教练（生病 / 出差对排计划尤其关键）。
- **目标赛事倒计时**：未来目标赛事卡加绿色小标（"还有 X 周 / X 天 / 就在今天"）。
- **保存 / 删除不再卡顿**：手动添加点保存表单立刻关、活动条立刻出现，天气和云端写入挪后台；删计划 / 删赛事 / 清空对话也即时生效（写失败才回滚）；导入计划到日历不再等弹窗。
- **设置页重组成三大块**：账号 / API / 其他。改密码加校验并改成弹窗（先输当前密码）；彩云天气可填自己的 token 走自己配额（留空用共享）。
- **日历下方 7 天天气卡**：格子里小图标去掉、更干净，下方新增 7 张天气卡（今天 + 未来 6 天，两行信息），今日卡修好了温度 / 体感空白，加「更新于 HH:MM」+ ↻ 刷新；新增[天气](weather.md)手册章节。
- **定位整合进个人资料**：原来分两处（资料里的城市 + 设置里的默认位置坐标）统一到个人资料的「位置」一处。点「📍 用当前位置」自动定位并按语言填好区 / 市级地址 + 存坐标，同时喂天气和教练。
- **全局字体焕新**：换成更圆润、有"运动手表"感的 Outfit（数字等宽对齐，中文不受影响）。
- **赛事文案 & 录入**：Half Marathon → Half Mar.、Marathon → Full Mar.；斯巴达 tier 上官方色（Sprint 红 / Super 蓝 / Beast 绿 / Ultra 紫）；GAP 拆成分 + 秒两个框。
- **Training 顶部 & 可读性**：手机端顶部从三行压成两行；类型筛选改名 All Types / 全部类型；长标签不再全大写；底部 5 个 tab 图标文字调大。
- **切换记忆**：sub-tab 选择会记住（比赛看着"历史"切走再回来仍停"历史"，Training 的 Activities / Charts 同理），彻底关掉重开才恢复默认。
- **个人资料未保存提醒**：改了没保存就按返回 / ✕ / 点外面会先确认「放弃未保存的修改？」。
- **APP 图标 / 开屏 / 返回键**（v0.3.0–v0.3.1 原生层）：图标满幅铺到边；开屏从两段式合并成一页；返回键改成"先关弹窗 → 回 Training 页 → 退后台不杀 App"。**应用内手册 + 应用内更新**（v0.3.2）：Guide 搬进应用离线可看、宽表自动转竖排卡片；检查到新版应用内下载 APK 并直接拉起安装、失败回退浏览器。
- **更新下载进度条**：检查更新 → 下载安装时按钮下方显示实时百分比（仅 APK）。
- 修复："从对话更新记忆"安卓后台不再失败；新加训练"保存中"暂时禁用编辑（约 1–3 秒）；安卓"检查更新"不再把预发布版误判成最新而漏报新正式版。

## 2026-05-28

- **手动添加训练自动抓天气快照**：保存时抓当时天气（温度 / 体感 / 湿度 / 状况 / 风 / AQI）存进记录；新增可选「开始时间」字段（留空抓此刻，填了抓那个时间点的历史或预报）；卡片行尾显示天气图标 + 温度，展开看完整数据。
- **日历天气**：每个格子（今天 + 未来 7 天）右上角显示天气图标和温度；点开某天弹窗顶部有当日天气总结（过去显示当天第一条训练录到的天气，今天 / 未来显示实时 / 预报）。
- **AI 教练读天气**：prompt 新增当前实时、最近 10 条训练各自当时、未来 7 天计划训练的天气，能基于"今天 32°C 体感 38°C"建议配速补给。
- **默认位置设置**：设备无法提供 GPS 时用默认坐标兜底，可一键从当前设备获取（安卓装了定位插件，首次抓天气弹权限请求）。
- 排序菜单在手机上更紧凑；修复安卓 App 内天气加载失败。数据源是彩云天气，由服务端代为拉取。

## 2026-05-27

- **安卓 APP 封装（v0.1.0）**：训练日志正式有了安卓 APK，不再只能用浏览器；预装完整页面和图标，首次启动不需联网拉资源（同步数据和 AI Coach 仍需网络）。设置 → 应用 → 检查更新可看版本号、下载 APK，覆盖升级不用先卸载。
- **Claude 可用 + 模型锁定最强**：教练可在 DeepSeek 和 Claude 之间切换；模型锁定为各家最强款、用户不用选；顶部加价格对比卡片。
- **AI Coach 回复支持 Markdown 渲染**：表格 / 粗体 / 标题 / 列表 / 代码 / 引用以前是字面字符，现在正常渲染；7 列周计划真正显示成表格、移动端太宽可气泡内横滑；长 URL / 模型 ID 自动换行。
- **AI Coach 体验**：发消息不再卡 1–2 秒（按下即出现）；每条用户消息下加「↻ 重新发送」；网络错误文案改写；移动端气泡变宽；日历导入按钮可在设置里隐藏。对话框上方加状态 chip 显示 Provider / 模式 / Memory / 导入按钮状态。
- **跑步类型推荐改成个性化心率区间**：填好平均心率后按你个人 5 区推荐配速子类（Easy / Aerobic / Tempo），匹配的 chip 带「推荐」+ 绿描边（只是建议，可选别的）；Z4+ 不主动推荐；没填 Resting / Max HR 退回旧统一阈值；CSV 导入的自动预填同理。
- **Training 统计重排**：从 Sessions / Distance / Ascent / Avg HR 改成 Sessions / Time / Distance / Ascent（总时长比平均心率有用），移动端 Time 简化显示。
- **Charts 简化**：默认窗口 8 周→4 周；删除「心率区间分布图」（按平均心率分桶会误导）；力量时长按部位比例分配（选 Upper Body 时一次"上肢+下肢"30 分钟只计 15 分钟）。
- **UI 操作区打磨**：底部导航和顶部工具条都加图标 + 文字，Training 的 Add / Upload / Select / Sort 集中成一行。

## 2026-05-26

- **顶部覆盖修复**：Training / Races 滚动时上面不再露出活动记录的"缝"。
- **训练卡片紧凑指标重排**（手机端）：时长统一放第一列、所有类型对齐，方便竖向扫读。
- **AI Coach 更不容易装睡**：每次回复前重新拉最新训练数据，系统提示词加硬指令明确"`[Recent Activities]` 就是事实、不准瞎说看不到"。
- **修改密码**入口上线（电脑右上角账号名菜单 / 手机 Settings tab）。
- 应用图标改为自定义图片；手册改成双端通用版本。

## 2026-05-25

整整一天磨手机体验：

- 移除顶部 header（手机端）省屏幕高度；新增手机专属 Settings tab，把桌面顶部的个人资料 / API / 语言 / 退出登录搬过来。
- AI Coach 的 Coach Config / Memory / Prompt 预览改模态弹窗；输入框固定底部、不被键盘挤走，旁边加 ⚙ 进设置。
- 活动卡片重新设计（固定 2 行、点开展开更多、长按选中）；日历从圆点改横条显示训练量。
- Training 顶部筛选改 All activities ▼ 下拉 + 4 列紧凑统计条；Races 拆成 PR / Races 两个 tab，各分 Target / History、标签带计数、sticky 标签头。
- ⭐ 可添加到主屏当 app 用，离线也能打开应用框架。

## 2026-05-22

- ⭐ **用户使用手册首次发布**，覆盖六种活动类型、配速分类、数据导入、赛事 + PR、AI 教练、图表；产品顶部加 Guide 入口。
- ⭐ **移动端独立 shell + 底部 5 个 tab 首次上线**（之前手机用的是桌面布局缩小版）。
- AI 教练数据块优化：当前时间用本地时间、赛事按类别精选、单位格式规范化；回答输出上限拉到 8000 tokens 不再中途截断。
- 赛事列表按列对齐、可按类别多选筛选。

## 2026-05-21

- ⭐ **日历功能上线**（当天从子视图升成独立 tab）：月历显示已完成 + 计划训练。
- ⭐ **AI 教练可一键把建议导入日历**（每条回复后的 📅 按钮）。
- 斯巴达细分 tier（Sprint / Super / Beast / Ultra）；PR 栏从顶部独立卡片并进 Races tab。
- 首次支持手机 / 平板响应式布局；删掉 `.fit` 支持（当时用户都用 CSV）；加 favicon。

## 2026-05-20

整整一天搬家——此前数据只存在你打开过的那个浏览器里、换设备就丢；当天起每个账号的数据上云，换设备能继续用。

- 登录系统上线（邮箱 + 密码，必须登录才能用）。
- 个人资料 + 偏好、训练记录 + 赛事、AI 教练对话历史都搬上云（错误气泡仍只存本地）。
- 清理旧的本地数据迁移代码。

## 2026-05-19

把"长得像 prototype"变成"长得像产品"：

- ⭐ 整体视觉风格定调（仪表 / 地形学 / 苔藓绿 + 极简色彩，此后没再大改）。
- ⭐ AI 教练长期记忆上线（多次对话间记住持久偏好）。
- ⭐ 心率区间图表上线（按个人 Resting / Max HR 算 Z1–Z5 时间分布）；图表新增"按时长划分"（路跑配速分布从按次数改按时长加权）。
- 赛事表单按类别动态显示字段（路跑不显示爬升、Hyrox 不显示距离、斯巴达只选 tier）；prompt 预览支持中英切换。
- 活动行列对齐、字号更易读；删掉用了两天没人用的"赛事网络搜索"。

## 2026-05-18

- ⭐ 个人心率区间上线（Karvonen / Standard 5-Zone 两种方法）。
- 活动类型补齐到六种（Road Run / Trail Run / Hiking / Floor Climbing / Strength / HIIT）。
- 赛事可以编辑了（之前只能增删）；表单点外面自动取消（有未保存改动会确认）。
- 修了几个空字段 / 距离单位 / 类别兜底的 bug。

## 2026-05-17

- 加了"用网络搜索给赛事建议"功能（独立 API）；用了两天没价值，5-19 删掉。

## 2026-05-16

- ⭐ 中英双语切换上线。
- 个人档案加姓名字段（页面标题显示）；居中布局。
- 数据导入进阶字段支持：步频、GAP、有氧 TE。

## 2026-05-15

**毛坯房状态**：最早只是一版能手动记录训练的雏形。当天能做的：手动添加几类训练 + 目标 / 历史比赛、跟一个 AI 教练对话（不带资料 / 记忆）。还没有：登录、国际化、图表、心率区间、日历、PR 栏、移动端、手册、应用图标、自动导入、爬楼 / HIIT / Strength 类型……11 天后长成你现在看到的样子。

## 2026-05-13

某天在网页端闲来无事点了下 Claude 的 **artifacts**，随手让它生成一个记录运动的小网页，这个 app 就那么诞生了。当时只是个跑在对话框里的玩具：没有登录 / 云端 / 手机版，刷新就没。但骨架（手动记训练、记比赛、跟 AI 教练聊两句）已经在了。两天后搬成正经项目，然后一发不可收拾。
